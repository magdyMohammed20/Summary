(1) Type Assertion
--------------------
==> Is Copy Variable Type From Another Variable With Some Restriction

EX
----

    let x: any = 10;

    // Means That y Has Same Type Of x But Must Be Number Only
    let y = <number>x // First Syntax
    let z = x as number; // Second Syntax