(1) Union
------------
==> Make Variable Accept Multi Specific Types

EX
---

    export { };
    // n Accepts Only 'number' Or 'string'
    let n: number | string;
    n = 10;
    n = "Magdy"

    // a Accepts Only 'boolean' Or 'Date'
    let a: boolean | Date;
    a = true
    a = new Date()